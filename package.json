{
  "name": "lockphone",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace=@lock-your/api && npm run build --workspace=@lock-your/web",
    "dev:api": "npm run dev --workspace=@lock-your/api",
    "dev:web": "npm run dev --workspace=@lock-your/web",
    "lint": "npm run lint --workspace=@lock-your/api && npm run lint --workspace=@lock-your/web",
    "test": "npm run test --workspace=@lock-your/api"
  },
  "engines": {
    "node": ">=22"
  }
}
